# Supply-chain hardening.
# Pin exact versions when installing new packages (no ^ / ~ ranges).
save-exact=true
# Fail the install if package.json and package-lock.json disagree, so the
# committed lockfile (with integrity hashes) is always the source of truth.
# Note: `npm ci` enforces this strictly — prefer it in CI and fresh installs.
